# RISC Zero Fallback

Fallback path only.

Use this if the Noir/UltraHonk verifier is not stable on Stellar by the June 23 gate. The guest must verify the same public journal fields as the Noir circuit and the Soroban account interface must stay unchanged.
